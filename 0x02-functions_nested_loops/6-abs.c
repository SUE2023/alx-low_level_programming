#include"main.h"
/**
 * _abs - computes the absolute value of an integer
 * Description: computes the absolute value of an integer
 * @int: the input
 * Return: (0)
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r = -1);
	}
}
