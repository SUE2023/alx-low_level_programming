#ifndef _MACROLIKE_H_
#define _MACROLIKE_H_
/**
 * File: 3-function_like_macro
 * @x: input value (an int)
 * Description: header file that compute absolulte value
 */

#define ABS(x) ((x) < 0 ? -(1) * (x): (x))

/* : act as else, ? means multiply by -1 or number to neutralize the negative */

#endif
